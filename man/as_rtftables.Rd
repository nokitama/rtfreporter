% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/as_rtftables.R
\name{as_rtftables}
\alias{as_rtftables}
\title{Convert a table object into rtfreporter table pages}
\usage{
as_rtftables(
  x,
  read = TRUE,
  max_rows = NULL,
  split = c("none", "rows", "group_safe", "group_force", "by_value"),
  split_rows = NULL,
  group_col = NULL,
  cont_label = " (Cont.)",
  blank_rows = NULL,
  blank_row_first = FALSE,
  blank_row_end = FALSE,
  align_count_pct = FALSE,
  border = "tfl",
  style = NULL,
  ...
)
}
\arguments{
\item{x}{A `gt_tbl`, a gtsummary table, a `data.frame` / tibble, or a
`list` of these.}

\item{read}{Passed to the gt metadata reader: `TRUE` (default, read
everything), `FALSE` (treat as a bare data.frame), or a character
vector of tokens.  Ignored for non-gt inputs.  See [as_rtftable()].}

\item{max_rows, split, split_rows, group_col, cont_label, blank_rows, blank_row_first, blank_row_end, align_count_pct}{Pagination controls.  Identical meaning to the (now deprecated)
`paginate()`.  `split = "none"` (default) keeps the whole table as a
single page.}

\item{border, style}{Passed to [rtftable()] for every page.  `border`
defaults to `"tfl"`.}

\item{...}{Further arguments forwarded to [rtftable()] for every page
(e.g. `col_header`, `col_spec`, `col_rel_width`, `row_height_twips`).
Explicit values always win over the gt-extracted ones.}
}
\value{
A list of `rtftable` objects, one per page.  When the split is
  value-based (or the input was a named list) the list is named.
}
\description{
`as_rtftables()` is the single entry point for turning a *table object*
into a list of [rtftable()] objects -- one per RTF page -- ready to hand
straight to [rtf_tables()].  It unifies two jobs that used to be split
across `paginate()` (page splitting) and `rtf_tables(read_gt = )`
(metadata extraction):
}
\details{
1. **Read the table's metadata.**  For a `gt_tbl` (or any gtsummary
   table, converted first via `gtsummary::as_gt()`), the column labels,
   per-column alignment, spanning headers, widths, hidden-column removal,
   stub / row-group structure, per-cell styles (bold / italic / underline
   / indent), in-cell footnote marks, title / subtitle and source notes
   are all extracted.  See [as_rtftable()] for the token list.
2. **Paginate.**  The rendered body is split into per-page chunks using
   the same strategies the old `paginate()` offered (`split`, `max_rows`,
   `group_col`, blank-row controls, ...).  Per-cell styles are sliced to
   match each page; the shared header / width / spanning metadata is
   replicated onto every page.

The page-level title / source-note blocks travel with each returned
rtftable as the attributes `rtf_titles` / `rtf_footnotes`, which
[rtf_tables()] consumes automatically -- so no `read_gt` flag is needed
anymore.

Supported inputs: `gt_tbl`, gtsummary tables, plain `data.frame` /
tibble, or a `list` of any of these (the list is flattened, names
propagated as `name`, `name.1`, `name.2`, ...).  Figures are out of
scope -- use [rtf_figures()] for those.
}
\examples{
\dontrun{
library(gtsummary)
tbl <- trial |>
  tbl_summary(by = trt) |>
  as_rtftables()                       # list of rtftable pages

doc <- rtf_document() |>
  rtf_section(page = 1, secinfo = list(header = NULL, footer = NULL)) |>
  rtf_tables(tbl)                       # titles / footnotes flow through
generate_rtfreport(doc, "out.rtf", overwrite = TRUE)
}

}
\seealso{
[as_rtftable()] for the single-page convenience wrapper,
  [rtf_tables()] to append the result to a document.
}
