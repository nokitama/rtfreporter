% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrappers.R
\name{rtfplot}
\alias{rtfplot}
\title{Create an RTF figure object}
\usage{
rtfplot(path, width_twips = NULL, height_twips = NULL, align = "center")
}
\arguments{
\item{path}{Path to a PNG or JPEG image file.}

\item{width_twips}{Display width in twips. `NULL` = full writable width.}

\item{height_twips}{Display height in twips. `NULL` = derived from aspect ratio.}

\item{align}{Horizontal alignment: `"center"` (default), `"left"`, or `"right"`.}
}
\value{
An `rtfplot` (S3) object suitable for use in `rtf_tables()`.
}
\description{
Embeds a PNG or JPEG image into the RTF output.
The result can be passed directly to `rtf_tables()` in a pipe chain.
}
\examples{
\dontrun{
fig <- rtfplot("scatter.png", width_twips = 9000L)

doc <- rtf_document() \%>\%
  rtf_section(page = 1, secinfo = list(
    header = rtf_header(rows = list(c(l = "Figure 14.1")))
  )) \%>\%
  rtf_tables(list(fig))

generate_rtfreport(doc, "output.rtf", overwrite = TRUE)
}

}
