% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paginate.R
\name{page_split}
\alias{page_split}
\alias{page_split_none}
\alias{page_split_rows}
\alias{page_split_group_safe}
\alias{page_split_group_force}
\alias{page_split_by_value}
\title{Built-in pagination strategies as reusable functions}
\usage{
page_split_none()

page_split_rows(split_rows = NULL)

page_split_group_safe(
  max_rows = NULL,
  group_col = NULL,
  min_group_rows = 2L,
  cont_label = " (Cont.)"
)

page_split_group_force(
  max_rows = NULL,
  group_col = NULL,
  min_group_rows = 2L,
  cont_label = " (Cont.)"
)

page_split_by_value(
  group_col = NULL,
  max_rows = NULL,
  min_group_rows = 2L,
  cont_label = " (Cont.)"
)
}
\arguments{
\item{split_rows}{Integer positions to cut at (for [page_split_rows()]).}

\item{max_rows}{Maximum data rows per page.  Required for
[page_split_group_safe()] / [page_split_group_force()]; optional for
[page_split_by_value()] (force-splits an over-long group when set).}

\item{group_col}{Group column: a name, a 1-based index, or `NULL` to
auto-detect groups from leading whitespace in the first column.}

\item{min_group_rows}{Widow/orphan control (default `2`); see
[as_rtftables()].}

\item{cont_label}{Continuation suffix for repeated group labels (default
`" (Cont.)"`).}
}
\value{
A function `f(df, ...)` returning a list of per-page data.frames.
}
\description{
These factories return a *pagination function* suitable for the
`split=` argument of [as_rtftables()].  They expose the package's built-in
page-splitting strategies on the same footing as a hand-written custom
`split=` function, so you can pass a strategy directly, reuse it across
calls, or wrap one inside your own splitter.
}
\details{
The string forms accepted by [as_rtftables()] are exact aliases:
`split = "group_safe"` is equivalent to
`split = page_split_group_safe()` with the call's `max_rows` / `group_col` /
`min_group_rows` / `cont_label`.

Each returned function takes the (cell-formatted) body and returns a list of
data.frames -- one per page (named, for [page_split_by_value()]).  Arguments
set on the factory take precedence; anything left `NULL` falls back to the
value [as_rtftables()] passes at call time.
}
\examples{
\dontrun{
as_rtftables(tbl, split = page_split_group_safe(max_rows = 20,
                                                 group_col = "visit"))
}

}
\seealso{
[as_rtftables()] for the `split=` contract and [add_cont_label()].
}
